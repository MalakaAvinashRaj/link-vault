# PostgreSQL connection string (Railway, Supabase, or any PostgreSQL host)
# Example Railway format: postgresql://postgres:<password>@<host>:<port>/<db>
DATABASE_URL="postgresql://postgres:password@localhost:5432/link_vault"
